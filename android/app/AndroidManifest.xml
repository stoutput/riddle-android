<?xml version="1.0" encoding="utf-8"?>
<!-- versionCode/versionName are deliberately absent: aapt2 injects them from
     the build (which reads the version from riddle-core/Cargo.toml). A literal
     value here would silently take precedence and freeze the version. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stoutput.riddleandroid">

    <!-- The oracle is an HTTP(S) endpoint; the diary needs nothing else. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The diary is a pen-first app: declare it so a device with a stylus
         exposes it as one, and so the Play listing/large-screen heuristics
         treat it as an e-ink/tablet app rather than a phone app. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch.distinct" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|uiMode"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Not exported: it is only ever opened from the diary. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|uiMode"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>
